{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hedgelion.invalid/schemas/interpreter-result.schema.json",
  "title": "HDM typed InterpreterResult",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "purpose", "source_generation", "intent"],
  "properties": {
    "kind": {"const": "interpreter_result"},
    "purpose": {"type": "string", "minLength": 1},
    "source_generation": {"type": "string", "minLength": 1},
    "intent": {"type": "string", "minLength": 1}
  },
  "examples": [{"kind": "interpreter_result", "purpose": "interpret", "source_generation": "frontier-7", "intent": "move"}]
}
