{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hedgelion.invalid/schemas/world-mission-state.schema.json",
  "title": "HDM world.mission state",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "status"],
  "properties": {
    "name": {"type": "string", "minLength": 1},
    "status": {"type": "object"},
    "stages": {"type": "object"},
    "participant_ids": {"$ref": "#/$defs/idSet"},
    "location_ids": {"$ref": "#/$defs/idSet"},
    "reward_ids": {"$ref": "#/$defs/idSet"},
    "dependencies": {"$ref": "#/$defs/idSet"},
    "details": {"type": "object"}
  },
  "$defs": {
    "id": {"type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_.:-]*$"},
    "idSet": {"type": "array", "uniqueItems": true, "items": {"$ref": "#/$defs/id"}}
  }
}
