{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hedgelion.invalid/schemas/world-scene-state.schema.json",
  "title": "HDM world.scene state",
  "type": "object",
  "additionalProperties": false,
  "required": ["name"],
  "properties": {
    "name": {"type": "string", "minLength": 1},
    "location_id": {"$ref": "#/$defs/id"},
    "participant_ids": {"$ref": "#/$defs/idSet"},
    "focal_entity_ids": {"$ref": "#/$defs/idSet"},
    "status": {"type": "object"},
    "details": {"type": "object"}
  },
  "$defs": {
    "id": {"type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_.:-]*$"},
    "idSet": {"type": "array", "uniqueItems": true, "items": {"$ref": "#/$defs/id"}}
  }
}
