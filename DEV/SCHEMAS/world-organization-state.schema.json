{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hedgelion.invalid/schemas/world-organization-state.schema.json",
  "title": "HDM world.organization state",
  "type": "object",
  "additionalProperties": false,
  "required": ["name"],
  "properties": {
    "name": {"type": "string", "minLength": 1},
    "member_ids": {"$ref": "#/$defs/idSet"},
    "leader_ids": {"$ref": "#/$defs/idSet"},
    "location_ids": {"$ref": "#/$defs/idSet"},
    "resources": {"type": "object"},
    "status": {"type": "object"},
    "details": {"type": "object"}
  },
  "$defs": {
    "id": {"type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_.:-]*$"},
    "idSet": {"type": "array", "uniqueItems": true, "items": {"$ref": "#/$defs/id"}}
  }
}
