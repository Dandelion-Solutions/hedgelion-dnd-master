# R2.7 WP-07 — Task-Brief Critic

Status: **STEP-1 CRITIC PASS — REPAIRS APPLIED**

## Mandate

This critic independently challenged the WP-07 Task Brief against the current
repository map and direct owners. Its purpose is to detect missing source
families, duplicate-authority assumptions and omitted consumers before Step 2.

## Critic result

The initial Brief correctly identified the central five-way information boundary,
but its first Source Manifest omitted three indirect dependency families:

1. Step 5.13 controls message-envelope retirement and survivor obligations after
   Step 5.11 compaction; it is therefore material to exact-history claims.
2. Recipient-scoped disclosure depends on authenticated player/session/interaction
   binding, not only on the abstract Step-5.12 rule.
3. SAVE/durability and runtime interaction/trace surfaces can preserve or retire
   communication evidence and therefore belong to the architecture-to-machine
   route.

The Task Brief was repaired before this verdict. No source family found by the
critic requires a change to WP-07's stated semantic boundary.

## Whole-project dependency check

| Dependency route | Critic disposition |
|---|---|
| Step-4 primary owners and the single-context amendment | retained as canonical and superseding semantic sources |
| Step-5 Story, Transcript, delivery, cleanup and integrated-review contracts | retained; Step 5.13 added explicitly |
| R2.1 continuity, R2.2 Actor cognition, R2.3 context runtime, R2.4 execution, R2.5 multiplayer and R2.6 host assurance | retained as direct boundary consumers |
| DEV catalog/entity/identifier contracts | retained as machine-contract owners; discrepancies must be reconciled against canonical semantics rather than by filename recency |
| GAME schemas, campaign template topology and CORE runtime instructions | retained as installed representation and consumer surfaces |
| player/session/interaction identity and authenticated binding | added explicitly |
| SAVE, durability, retention and cleanup | added explicitly |
| broad sources/history prose, generic GM advice and unrelated rules-domain material | reviewed as routing/context only; not a separate WP-07 semantic owner unless a concrete consumer edge is found |

## Required critic attacks for Step 2

Step 2 must specifically test:

- whether `GAME/SCHEMA/lore.schema.yaml` is an unmarked legacy representation
  inconsistent with the canonical truth/lifecycle split;
- whether `DEV/ARCHITECTURE/ENTITY_STRUCTURES.md` is stale relative to the
  current catalog field contract for `world.knowledge`;
- whether catalog admission/identifier policy is backed by the required physical
  representation or correctly classified as a deferred implementation obligation;
- whether PC/live/session fields are bounded projections or temporary
  epoch-local evidence rather than a second writable global owner;
- whether compaction and retention preserve required message, Transcript,
  disclosure and idempotency survivor obligations.

## Decision

No product-semantics, authority-change, compatibility-policy or risk-acceptance
decision is exposed by framing alone.

**Critic verdict: PASS.** The repaired Task Brief is review-ready for the
mandatory Senior Step-1 review.
