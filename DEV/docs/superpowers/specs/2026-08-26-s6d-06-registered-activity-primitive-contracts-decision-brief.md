# S6D-06 Step 3 — Decision Brief

## Established facts

There are exactly 31 registered primitives and no admitted seed consumer. Accepted architecture already assigns commit disposition to ExecutionSegment, committed fact to MechanicalEvent, outcome to receipt, and forbids independent Signal/StateDelta lifecycle state.

## Decision

Adopt an exact fail-closed quarantine disposition for every registered primitive. No executable contract is certified: current detailed rows are activation research only. A future seed domain must replace and re-review the relevant row as `COMPLETE`, prove its exact consumer and active dependency closure, and pass catalog admission before selection.

## Consequences

The compiler unconditionally rejects unknown and quarantined primitives; no override flag may activate them. No query language, event queue, generic delta, implicit RNG or global Activity atomicity is introduced. This is an evidence-constrained technical closure and requires no product decision.

