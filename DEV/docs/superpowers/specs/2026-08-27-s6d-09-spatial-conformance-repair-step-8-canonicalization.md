# S6D-09 Spatial Conformance Repair — Step 8 Canonicalization

Status: **COMPLETE / CANONICAL / PUBLISHED AND READ BACK**

Date: 2026-08-27

## Canonical delta

This Step 8 incorporates the narrow Senior-audit spatial repair into the existing S6D-09 canonical result. It does not alter Decision C or begin S6D-10.

Canonical owners and machine contracts are synchronized as follows:

- `DOMAIN_RULES_COVERAGE.md` separates target applicability, Procedure movement and durable location transition;
- `MECHANICAL_CONTEXT.md` and `mechanical-surfaces.json` admit `fiction.target_reachable` only for seven exact consumers while leaving `fiction.target_visible` dormant;
- `PORTABLE_ACTIVITY_VALUES.md` remains the TargetSpec/AreaSpec and embedded invocation-fact owner; the strict envelope now realizes consumer/binding/rules-context identity without lifecycle state;
- `ENTITY_STRUCTURES.md` preserves `Actor.location_id` as a durable world-location reference and excludes tactical micro-position overload;
- the primitive contract replaces the unowned geometry-infrastructure read with exact accepted-fact and role reads, without changing primitive activation inventory;
- the package seeds, schemas, reference validator, tests, evidence matrix and package identity agree on the repaired route;
- Step-4/5 historical spatial statements are explicitly superseded;
- `PROJECT_MAP.md` and the roadmap route future work to this repaired closure.

## Publication and read-back result

The complete delta was assembled from the re-read current head, verified on a connector staging ref and then fast-forwarded atomically to authoritative ref `v1/engine-rearchitecture` without force. Authoritative branch read-back matched the published head; 28 local canonical/machine artifacts matched their remote contents, five additional remote owners/map/roadmap amendments contained the required spatial laws, six representative authoritative files were re-read successfully, package/coverage verification passed and the focused S6D-06…09 suite passed 58/58 against byte-equivalent staging.

S6D-10 remains not started and is outside this canonicalization.

