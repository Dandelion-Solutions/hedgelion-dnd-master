# R2.7 WP-09 — Context loading, retrieval and resource-bounded operation — mini-report

## Статус

**STEP 2 IN PROGRESS — EVIDENCE SLICE 1: CANONICAL OWNERS COMPLETE**

Глобальная авторизация и gate принадлежат `DEV/CURRENT_PROGRESS.md`. Этот
mini-report — task-local evidence/cursor WP-09, а не global current-progress
authority.

## Краткий вывод

Первичные owners уже фиксируют bounded multi-channel discovery, packet-first
allocation, legal representation floors, routed currentness/eligibility, central
conservative estimator и finite outcomes. Это не оставляет открытой product или
architecture альтернативы на Step 2. Текущие GAME/DEV consumers ещё должны быть
проверены в обратном направлении.

## Покрытые вопросы

- bounded discovery before full load;
- routing-only scene/location/index hints;
- packet floors, optional degradation и `UNSATISFIABLE`;
- отсутствие exact hidden token telemetry;
- no global/history preload;
- ephemeral Context Runtime versus durable/HOT support;
- currentness/eligibility, recipient catch-up и bounded history orientation.

## Source Manifest delta

Прочитаны actual primary owners R2.1, R2.3, R2.4, R2.5, R2.6, Step-4 amendment
и Step-5.14. Подробная item-level extraction:
`2026-08-31-r2-7-WP-09-step-2-canonical-owner-evidence.md`.

## Установленные факты

1. Requiredness и floors принадлежат registered consumer/profile, не LLM ranking.
2. Index/current/scene являются discovery/routing support; они не доказывают
   closed world и не заменяют current native owner.
3. `ASSEMBLED_DEGRADED` сохраняет все required semantics; `UNSATISFIABLE`
   terminal и требует finite registered caller path.
4. R2.6 запрещает exact hidden token telemetry; calibration — post-implementation
   evidence, не причина для parallel MVP.
5. Context/trace/control не становятся durable authority; SQLite/HOT format сам
   по себе не authority.

## Architecture -> machine

Нужна точная проверка current GAME/DEV consumer graph. Пока не доказано, какие
surface уже частично поддерживают contract и где находятся gaps.

## Machine -> architecture

Не начато для current consumer graph.

## Конфликты / stale / negative findings

Нет material contradiction. Index partitioning остаётся CONDITIONAL / DORMANT
до доказанного R2.3 trigger. Generic memory/graph/worker/provider-cache не
авторизованы.

## Автоматически принятые технические решения

Не принимать current cache/index/schema за authority по названию или наличию;
проверять каждый consumer against R2.3/R2.4/R2.6 и downstream boundaries.

## Implementation obligations

Не сформированы до reverse audit.

## Verification / MVP acceptance obligations

R2.6 уже требует future behavioural proof floors/degradation/`UNSATISFIABLE`;
конкретный current test mapping ещё открыт.

## Forward obligations

Пока нет. Physical topology/HOT details остаются evidence boundaries WP-11/WP-12
и будут routed только при material finding.

## Human decision

**NONE.**

## Точка продолжения

Step-2 slice 2: current GAME CORE, schemas/templates and routing consumers.
