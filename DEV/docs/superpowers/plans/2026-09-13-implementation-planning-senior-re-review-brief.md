# HDM Implementation Planning — Independent Senior Re-review Brief

Status: **READY FOR GENUINELY INDEPENDENT SENIOR RE-REVIEW**
Date: 2026-09-13
Author repair control-state head before this brief: `e5979c3a4aa09c57c2d760d3c5e44da40151b642`.
First independent reviewed head: `38f4eb527fbbbd3a03e92aed4bf1e7315346cd21`.
Production implementation authorized: **NO**.

## Role

Act as a genuinely independent HDM Senior Implementation Plan Reviewer. Verify the repaired package and do not repair it while reviewing. The first independent result and author repair closure are evidence to challenge, not authority for PASS.

## Staged evidence protocol

Start with:

1. fresh branch HEAD;
2. `DEV/CURRENT_PROGRESS.md`;
3. current review/authority rules from `AGENTS.md` and the applicable runtime overlay;
4. this brief;
5. `2026-09-13-implementation-planning-independent-senior-review-result.md` — SIP-001..SIP-011 and verdict;
6. `2026-09-13-implementation-planning-senior-review-repair-closure.md`;
7. `2026-09-13-implementation-planning-package-index.md`;
8. repaired execution waves, bidirectional coverage and lossless-proof control ledger.

Do not bulk-read the repository. Use `DEV/PROJECT_MAP.md` only as a router. Retrieve readiness/spec/owner evidence by exact ID or exact disputed section. Broaden evidence only for drift, disagreement, ambiguity, authority-transfer risk, untraceable work, unproven dependency, current-body-dependent file action or finding confirmation.

## Currentness first

Compare `e5979c3a4aa09c57c2d760d3c5e44da40151b642` to re-review HEAD before deep review.

- Control-only delta: record `NO_SEMANTIC_OWNER_DRIFT` and continue.
- Canonical owner drift: identify the affected dependency subgraph and revalidate only that scope.
- Material package-wide drift: issue a finding rather than exhaustively reviewing stale plans.

Independently challenge the author's claim that the repair interval changed planning/control artifacts only.

## SIP-001..SIP-011 re-review matrix

### SIP-001 — allocator realization
Primary route: RD-04. Verify explicit campaign allocator state/operation/atomic-create behavior, transaction/HOT binding and focused negatives. No generic ID authority and no campaign allocator fallback for source-native LIVE identities.

### SIP-002 — surviving stale consumers
Verify each active stale consumer named by the first finding is explicitly classified and routed, including Location reverse-presence and STORAGE/recovery surfaces. A replacement schema elsewhere is not sufficient.

### SIP-003 — information normalization
Primary routes: RD-02 + RD-09. Verify an executable producer/consumer path from admitted LIVE/embedded evidence into native information relations while keeping lore, knowledge, disclosure, retained messages and history under their accepted authorities.

### SIP-004 — Actor behavior
Primary route: RD-03. Verify owner-local purpose/current-revision/evidence/`NO_CHANGE` behavior and continuity draft/validate/publish/promotion with no unresolved material worker design choice.

### SIP-005 — collaboration
Primary route: RD-12. Verify WP-17 obligation lineage, immutable IntentClause identity/content, hold/handoff, closed-set fingerprint, PLAYER routing companion, publication/recovery linkage, accepted collaboration modes and channel distinctions. Preserve scope-local waiting and no global active player.

### SIP-006 — history / Story / Commentator / T0 / Dramaturg
Primary route: RD-13. Verify native SemanticEvent/history authority, noncanonical Story, self-contained Commentator control/filter/cache path, sparse T0 evidence and exact multiplayer Dramaturg horizon generation/invalidation/privacy routes.

### SIP-007 — bootstrap/product
Primary route: RD-14. Verify gameplay-first provisional initialization without unconditional Story/T0 prerequisite, plus generator/identity propagation, initial-tree materialization, retrospective entry, save/menu behavior and creator fail-closed paths.

### SIP-008 — package-wide executability
Review the **current routed RD-01..RD-14 plans sequentially** using the package index. RD-08/RD-10/RD-11 must use their `-v2` routes.

For each RD retain a compact result covering exact files/interfaces, named RED cases, GREEN acceptance, REFACTOR boundary, focused VERIFY command, coherent passing checkpoint, Version/System Impact gate, currentness fence and any remaining material worker design choice.

### SIP-009 — lossless proof routing
Read the control ledger, then the WP-12/13, WP-14/15 and WP-16/17 appendices one at a time. Verify item-level proof duties remain individually traceable to named witnesses/supporting routes or owner-backed `NOT_APPLICABLE`. Future empirical/release branches must remain dormant.

Verify all nine pure-proof leaves and all eight composite parents. `133/133` identity accounting must not substitute for runtime/lossless-proof PASS. Composite closure must require all slices, joins, parent scenarios/negative laws and parent Version Impact reconciliation.

### SIP-010 — dependency/scheduling semantics
Primary route: repaired execution waves. Verify hard prerequisites, joins, integration-completion gates, scheduling preferences and shared-file checkpoints are distinct. Confirm RD-11/RD-12 core independence until R124 join and no shared-file checkpoint creates semantic dependency.

### SIP-011 — maintenance command
Audit current routed RD plans only. Where maintenance audit is invoked as a command, verify the executable form is `python3 DEV/TOOLS/run_maintenance_audit.py`. Historical naming outside current worker routes is not itself a defect.

## Canonical accounting and reverse coverage

Independently reconfirm:

```text
116 direct
9 pure proof
8 composite parents
= 133 active identities
12 trigger-gated outside current execution
79 explicit no-work
R004 absent
```

Then reverse-check current plans, proof routes and execution waves. Every executable task must trace to admitted readiness/slices, an owner-valid integration join, pure-proof route, Version Impact projection or required validation evidence. Report orphan work or architecture invention.

## Protected invariants

Adversarially check: no second gameplay/knowledge/history/currentness authority; eligibility before semantic use; ranking cannot override authority/eligibility/requiredness; TurnEnvelope control is not authority; physical presence is not eligibility; late steering is non-authoritative; only validated Narrator payload crosses `EMISSION_COMMIT`; no global active player; positive dependency before scope-local waiting; join/rejoin current frontier before mutation; catch-up is recipient projection; Story noncanonical; T0 not parallel history; Context Runtime ephemeral; CURRENT routing-summary only; no generic global chronology frontier.

## Verdict and publication

Report itemized `BLOCKING`, `SIGNIFICANT` and `MINOR` findings with exact evidence. Do not silently repair.

Final verdict must be exactly one:

- `PASS / GO` — no unresolved BLOCKING or SIGNIFICANT defect and package execution-ready;
- `FAIL / REPAIR REQUIRED` — otherwise.

A PASS advances the repository to its next gate; it does not itself perform implementation, migration, release or gameplay bootstrap.

Publish a new independent result under `DEV/docs/superpowers/plans/`, preferably `2026-09-13-implementation-planning-independent-senior-re-review-result.md`, containing review HEAD/baseline, currentness, SIP matrix, sequential RD status, proof/dependency/reverse-coverage results, evidence escalations, findings, final verdict and exact next authorized unit. Update `DEV/CURRENT_PROGRESS.md` only according to that independent verdict.

The re-review is complete when all eleven original findings plus package accounting/currentness/reverse coverage have been independently proved or disproved with the smallest sufficient authoritative evidence set.
