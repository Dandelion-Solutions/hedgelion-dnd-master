# HDM Implementation Planning Package — Master Plan

Status: **AUTHOR ADVERSARIAL CLOSURE ACTIVE**
Date: 2026-09-15

Current executable routing is defined by `2026-09-13-implementation-planning-package-index.md`.

Current package: RD-01 through RD-16; 38 mandatory overlays. Author Findings 1–31, F34–F48, F50, F52, F53, F54, F55, F57, F58 and F59 are planning-repaired and independently unconfirmed; F32 is a negative finding; F33, F45 and F49 are repaired control-plane defects. F45/F49 add no implementation edge or overlay. F50 is folded into existing mandatory overlay 11 and F52 is folded into existing mandatory overlay 28; neither adds overlay count.

Current execution authority: `2026-09-14-implementation-planning-execution-waves-v2-post-graph.md` plus all later-precedence package-index amendments through F55 plus F57's F36 verification-command correction. In particular: F34 adds the runtime-family proof join; F35 integrates the RD-12 PLAYER collaboration companion into RD-16 strict PLAYER state; F36 closes campaign<->LIVE temporal-companion handoff; F37/F38 close executable PLAYER access-transition and reverse collaboration reconciliation; F39–F41 close shared-writer/scaffold/documentation joins; F42 makes RD-02 the sole LoreFact/Knowledge strict-schema producer consumed by RD-16; F43 fixes deterministic retained-schema local-version cutovers; F44 removes the nonexistent historical R047 mutation target; F46 fixes deterministic CORE module revisions and adds one final physical/version integration sink each for shared `BOOTSTRAP_RUNTIME.md` and `STORAGE.md`; F47 adds no implementation ordering and binds F41's two README integration sinks to one exact terminal package-proof witness; F48 retires the stale manifest `players.player_ids` membership surface, keeps exact current PLAYER as the sole membership/authorization owner and extends the retained-schema proof sink with `campaign_manifest` v5; F50 adds no new execution edge and extends the existing SIRR2/F9 final `MULTIPLAYER.md` acceptance so join/rejoin uses `PRINCIPAL_PLAYER_ROUTING` plus exact current PLAYER reload rather than generic `PLAYER_INDEX` stable-principal lookup; F52 adds no new execution edge and strengthens existing RD-14/bootstrap and `RD14_INSTALL_BOOTSTRAP_FINAL_INTEGRATION` acceptance with a provider-independent bounded campaign candidate/card producer, bounded continuation/narrowing and final projection cutover away from exhaustive all-campaign card reads; F58 adds the native operational-root producer/contract/publication/recovery joins, fifth F40 blank input, bounded RD-07 storage-template input and terminal PG35 proof sink; F59 adds bounded access-policy mutation, common-after-view collaboration reconciliation, publication/consumer joins and terminal PG36 proof.

Current bidirectional coverage authority: `2026-09-14-implementation-planning-bidirectional-coverage-v3-post-graph.md` plus F27–F31 and all later-precedence item-bound amendments through F55 plus F57's F36 verification-command correction; F49 is control-plane only; F58/PG35 adds the bidirectional operational-root chain and F59/PG36 adds the existing access-policy mutation/consumer chain without new readiness IDs.

Current post-graph proof authority: historical v2 ledger/appendices + `2026-09-14-implementation-planning-lossless-proof-ledger-v3-post-graph.md` + F27–F31 control amendment + `2026-09-14-implementation-planning-post-graph-proof-witness-matrix.md` + F34–F55 later-precedence proof amendments plus F57's F36 verification-command correction. F47 specifically requires `SharedSchemaStorageReadmeIntegrationProofTests` over final integrated `GAME/SCHEMA/README.md` and `GAME/TEMPLATE/STORAGE_README.md` bytes, with `STATIC_AUDIT` as the primary channel; F48 extends retained-schema proof to `campaign_manifest` v5 and requires no `players.player_ids`; F50 requires final integrated `MULTIPLAYER.md` proof that join/rejoin consumes the F9 principal route and no longer instructs stable-user lookup through generic `PLAYER_INDEX`; F52 requires `DEV/TESTS/test_rd14_bootstrap.py::BoundedCampaignDiscoveryTests` plus final integrated bootstrap/install projection proof that menu discovery remains finite and never falls back to normal exhaustive `campaign/*` card traversal; F58 requires PG35 actual enrollment/publication/LIVE handoff/cold recovery, generated blank format and final projection witnesses at the exact modules/channels in overlay 37; F59 requires the four task-owned access-policy/reconciliation/publication/consumer classes in overlay 38 and extends exact R080 items 3/4/11/12 plus R083 theme16.

Highest-precedence LIVE realization repairs remain Findings 24–31, composed with F36 for temporal source handoff and F37/F38 when PLAYER authority mutations intersect LIVE/collaboration state, with F58 composing operational-root membership into the same exact LIVE acceptance and campaign handoff edges and F59 applying the full join to existing campaign access-policy mutations.

Current author-audit control rules:
- catalog generation remains `2` unless a separately proven incompatible released-generation boundary appears; current R2.7 is one unreleased clean-slate machine generation;
- F43/F48 exact retained-schema target versions are mandatory and obsolete pre-release shapes do not receive migration/dual-read/legacy-alias compatibility debt;
- F44 R047 is negative/currentness proof only; `GAME/CORE/DOMAIN_RULES_COVERAGE.md` must not be recreated solely to satisfy historical text;
- F46 closes the deterministic Category-B `framework_module_version` census for current mandatory material CORE edits: sixteen exact target rows, no speculative bumps for inspect-only files, and no per-RD double-bump when multiple owner deltas intentionally converge into one final unpublished v1 module edit;
- `BOOTSTRAP_RUNTIME.md` final target is `1.0.9` through the RD-01 + RD-14 CORE integration checkpoint; F52 folds into that same final unpublished edit and creates no second bump;
- `STORAGE.md` final target is `1.0.2` through the RD-04 + RD-07 CORE integration checkpoint;
- existing `MULTIPLAYER.md` shared integration remains one final target `1.0.8`, now also constrained by F50's principal-routing shipped-consumer cutover;
- F47 makes F41 integrated-byte proof non-optional: the final shared schema/storage README bytes require one exact package witness and cannot be closed by owner-local suites alone;
- F48 keeps exact current PLAYER as the sole membership/authorization owner, retires manifest `players.player_ids`, preserves manifest `players.join_policy`, and keeps campaign-card participant login projection non-authoritative;
- F50 forbids generic `PLAYER_INDEX` as stable-principal completeness/authorization route; normal join/rejoin uses current `PRINCIPAL_PLAYER_ROUTING` -> exact PLAYER reload and broad PLAYER/index scan is repair-only;
- F52 forbids normal menu discovery from traversing every physical campaign ref when the candidate set exceeds the admitted finite window; RD-14 must expose bounded candidate/page production, bounded card hydration, continuation/narrowing or typed bounded inability, and an exact-selector route that does not first enumerate the complete campaign namespace.

Finding 45: this master plan had remained frozen at the earlier 16-RD / 25-overlay / F36 state even though package index/progress had advanced to 16 RD / 33 overlays / F44. That stale derivative could misroute a reviewer or closure audit. Repair: this file delegates executable routing to the current package index and tracks the current package. F45 adds no product/runtime work, dependency edge, readiness ID or overlay.

Finding 46: deterministic material CORE edits were still partly left as worker-time version choices and two shared CORE modules lacked one final writer/version sink. Repair: `2026-09-14-implementation-planning-core-module-version-shared-writer-amendment.md` provides the exact sixteen-module cutover matrix, the two missing shared CORE joins and package-level module-version proof.

Finding 47: F41 required final integrated-byte proof for two shared README projections but supplied neither an exact executable package witness nor a primary proof channel, and the current proof router had no F41 row. Repair: `2026-09-14-implementation-planning-shared-readme-proof-routing-amendment.md` binds both F41 integration checkpoints to `SharedSchemaStorageReadmeIntegrationProofTests` and terminal `SHARED_SCHEMA_STORAGE_README_PROOF_READY`; the graph effect is proof-only.

Finding 49: after F48 this master again lagged the canonical package router/progress at 35 overlays / through F47. Repair resynchronized it to the unchanged 36-overlay package through F48. F49 is control-plane only and adds no product/runtime work.

Finding 50: the later F9 principal-routing repair did not propagate into the existing shared final-byte `MULTIPLAYER.md` cutover. Current shipped join guidance still told workers to search generic `PLAYER_INDEX` for a stable GitHub user binding even though that index has no stable-external-user key and F9 forbids it as completeness/authorization authority. Repair extends mandatory overlay 11 and the existing SIRR2 shared `MULTIPLAYER.md` checkpoint: join/rejoin must use current `PRINCIPAL_PLAYER_ROUTING` candidate routing, exact current PLAYER rehydration and fail-closed repair semantics; ordinary broad scans are forbidden. The final module target remains `1.0.8` and no new execution edge/overlay is added.

Finding 52: current owners WP24-11/WP19-L02 require bounded campaign discovery per menu operation, but RD-14 consumed `bounded_campaign_cards` without assigning a producer/continuation contract, while shipped `BOOTSTRAP_RUNTIME.md` still prescribed one `CAMPAIGN_CARD` probe per `campaign/*` ref. That left an `O(N)` remote-read route and forced the worker to invent the missing mechanism. Repair extends mandatory overlay 28: RD-14 must bind a provider-independent bounded candidate/page producer and bounded card hydration before selection, support continuation/narrowing or typed bounded inability, use an exact-selector fast path where available, forbid broad fallback, and cut final bootstrap/install projections away from exhaustive all-ref card reads. `BoundedCampaignDiscoveryTests` is mandatory; the existing F46 `BOOTSTRAP_RUNTIME` final target remains `1.0.9`; no new overlay or execution edge is added.

F53 is a minor planning-proof defect repaired inside mandatory overlay 28. F39/F52 already named the intended shared-writer and bounded-discovery checks, but did not explicitly assign their primary proof channels or locate the shared-writer class. The repair binds `DEV/TESTS/test_rd14_bootstrap.py::InstallBootstrapSharedWriterTests` to `STATIC_AUDIT`, separates `BoundedCampaignDiscoveryTests` behavioral cases (`FOCUSED_BEHAVIOR`) from final-file assertions, and requires the final install/CORE proof after the existing physical integration checkpoints. It adds no overlay, semantic execution edge or version bump.

F54 is a minor readiness-routing defect repaired inside mandatory overlay 34: the `RANDOMNESS.md` material-version row now points to `R043` (fixed RNG retention/recovery), matching the exact WP-27 Step-2 record and RD-01. `R050` remains the separate information/catalog prose duty. The final writer, target `1.0.3`, required RNG behavior and proof obligations are unchanged.

F55 is a significant shared-writer/proof defect repaired inside mandatory overlays 30 and 35. RD-08 v2 explicitly includes the schema README in its CURRENT/R010 checkpoint, but the F41 final writer and F47 witness admitted only RD-02/RD-03/RD-04/RD-07. The existing schema-README sink now also consumes bounded `RD08_SCHEMA_DOC_DELTA_READY`, and the same actual-byte static witness proves all five contributions. At the F55 boundary the storage-template sink retained three inputs; F58 later adds the bounded RD-07 operational-root input; no semantic owner, runtime dependency, version bump or whole-RD barrier is added.

F56 is a minor current-document namespace projection defect repaired in `DEV/ARCHITECTURE/CATALOG_CONTRACTS.md`, `CATALOG_ADMISSION.md` and `CATALOG_RESOLUTION.md`. Their active generation/package-field wording lagged the already approved and realized versioning policy. Current guidance now uses integer `catalog_generation: 2`, package revision and explicit compatibility family/generation; the recorded S6D-02 admission snapshot is distinguished from current machine-owned counts/selectability. No machine representation, generation value, implementation task, overlay or proof/execution edge changes.

F57 is a minor proof-command routing defect repaired inside mandatory overlay 25 (F36): its RD-06 verification command now invokes `DEV.TESTS.test_rd06_durability_publication`, matching the test module explicitly created by RD-06 and extended by the current LIVE amendments. The former command named an unplanned module. The handoff mechanism, required cases, test ownership, primary channels, checkpoint graph, versions and overlay count are unchanged.

F58 is a significant operational-root producer/planning defect repaired by mandatory overlay 37 (`2026-09-15-implementation-planning-operational-root-routing-addendum.md`). Step-5.2/R072 require independently recoverable Command/Procedure/promised unresolved input roots, but RD-07 consumed the set without a non-temporal enrollment producer or physical route. The repair assigns RD-05 native eligibility, RD-07 typed active-only membership and complete pinned recovery, RD-06 campaign closure, RD-09 LIVE CAS/selection/absorption, a fifth F40 blank-scaffold input, and PG35 exact integration proof. F41/F47 now integrate four storage-template inputs including bounded RD-07 operational routing; schema README retains five complete owner inputs. No semantic owner, readiness ID, native family, runtime implementation or additional version bump is introduced.

F59 is a significant access-policy mutation/planning defect repaired by mandatory overlay 38 (`2026-09-15-implementation-planning-campaign-access-policy-transition-addendum.md`). Existing creator-owned mode/join-policy and narrow PLAYER mechanical-override grant changes had validators and LIVE consumers but no exact-current mutation producer beyond F37's five lifecycle/control operations. RD-09 now plans typed field deltas and complete bounded impact; RD-12 reconciles each affected obligation once under a common after-authority view; RD-06/RD-07/LIVE and current planning/catch-up consumers supply the joined acceptance/recovery proof in PG36. Join-policy changes do not revoke existing bindings, grant revocation is prospective, and creator uncertainty still fails closed. No creator placement, new policy authority, durable schema, version bump or production implementation is introduced.

Current block: continue fresh graph-based adversarial review of the complete 16-RD / 38-overlay package. F59 publication does not constitute zero-open author closure. Remaining passes include residual proof/mechanism asymmetry, stale shipped-consumer reverse scan beyond already audited surfaces, recovery negative paths, worker-must-invent seams, runtime end-to-end/LLM/remote critical-path performance and final whole-checkpoint DAG/self-attack reconciliation.

Independent Senior review remains blocked until the adversarial graph audit reaches fresh zero-open author closure and the exact final HEAD passes hosted maintenance audit plus full DEV unittest discovery.

Production implementation, migration, release execution and gameplay bootstrap are not authorized.
