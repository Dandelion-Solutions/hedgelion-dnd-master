schema_name: campaign_manifest
schema_version: 1
required:
  - schema_version
  - campaign_id
  - campaign_name
  - branch
  - status
  - mode
  - engine
  - storage
fields:
  schema_version: integer
  campaign_id: string
  campaign_name: string
  branch: string
  status: enum[initializing, active, paused, archived]
  mode: enum[singleplayer, multiplayer]
  engine:
    base_tag: string
    base_sha: string
    integrated_main_sha: string
  rules:
    baseline: string
    house_rules_path: string|null
  storage:
    state_root: string
    index_root: string
    world_root: string
    event_log_root: string
    checkpoints_root: string
  sync:
    atomic_turn_commits: boolean
    force_push_allowed: boolean
  world_time:
    calendar_id: string|null
    frontier: object|null
  players:
    player_ids: array[string]
  last_checkpoint_id: string|null
  last_event_id: string|null
  created_at: string
invariants:
  - branch MUST start with 'campaign/'
  - force_push_allowed MUST be false for live campaigns
  - mode changes only by explicit user/admin instruction
  - base_tag/base_sha identify the engine release from which campaign was created
